services:
  - type: web
    name: boss-kaba
    env: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bosskaba-db
          property: connectionString
databases:
  - name: bosskaba-db
